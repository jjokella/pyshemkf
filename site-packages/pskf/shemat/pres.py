def head2pres(head, z, rref, grav, psurf):
    """
    Compute pressures from head.

    Parameters
    ----------
    head : Numpy array
        One/Two/Three dimensional array of equivalent head values.

    z0 : float
        z-variables of model, same dimension as head-array.

    rref : float
        Reference density of the fluid.

    grav: float
        Gravitational constant.

    psurf: float
        Surface pressure.

    Returns
    ----------
    pres: Numpy array
        Pressure top boundary condition [MPa]
    """
    dif = head - z

    pres = (psurf + dif * rref * grav) * 10**-6

    return pres


def backgroundpres(z, h0, rref, grav, psurf):
    """
    Compute background pressures for water table at h0.

    Parameters
    ----------
    head : Numpy array
        One/Two/Three dimensional array of equivalent head values.

    h0 : float
        height of water table above lower end of model.

    rref : float
        Reference density of the fluid.

    grav: float
        Gravitational constant.

    psurf: float
        Surface pressure.

    Returns
    ----------
    pres: Numpy array
        Pressure top boundary condition [MPa]
    """
    dif = h0 - z

    pres = (psurf + dif * rref * grav) * 10**-6

    return pres
